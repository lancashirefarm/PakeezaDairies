<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="custom_login.list">
      <field name="name">custom_login list</field>
      <field name="model">custom_login.custom_login</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="custom_login.action_window">
      <field name="name">custom_login window</field>
      <field name="res_model">custom_login.custom_login</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="custom_login.action_server">
      <field name="name">custom_login server</field>
      <field name="model_id" ref="model_custom_login_custom_login"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="custom_login" id="custom_login.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="custom_login.menu_1" parent="custom_login.menu_root"/>
    <menuitem name="Menu 2" id="custom_login.menu_2" parent="custom_login.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="custom_login.menu_1_list" parent="custom_login.menu_1"
              action="custom_login.action_window"/>
    <menuitem name="Server to list" id="custom_login" parent="custom_login.menu_2"
              action="custom_login.action_server"/>
    -->
  </data>
</odoo>